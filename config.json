{
  "schema": 1,
  "week_id": "2026-07-12",
  "modifiers": ["crowded_court", "glutted_maw"],
  "stipend": 999,
  "_comment": "TEMPORARY D6 VERIFICATION OVERRIDE, NOT SHIPPABLE. week_id 2026-07-12 is window 131's UTC start, so this MATCHES the live window and the fetch path applies it instead of falling through to derivation. stipend 999 is the observable: derivation hardcodes 240, so 999 in the sealed wallet proves the transport fetched, validated, and applied. Modifiers are unchanged from the kill-switch (already known-legal; they are not the observable). RESTORE THE KILL-SWITCH AFTER THE TEST. This override self-expires when window 132 opens 2026-07-19 07:00 UTC, after which week_id no longer matches and every client falls through to derivation."
}
